# CLAUDE.md — busy-office-engine

Bootstrap scaffold only (extracted from busy-office-erp issue #1). No
round-table has run in this repo yet — nothing here is a ratified
decision, per the same Rule Zero discipline as busy-office-erp
(see that repo's CLAUDE.md / DESIGN-GRAPH.md for the pattern this repo
will eventually adopt for its own graph).

Consumers: busy-office-erp pins this repo by version (ADR-11, in the erp
graph). Until transport v0 exists, erp vendors its own copies of
`lints/graph-lint` and `lints/trust-lint` locally — see erp issue #1 for
the cutover plan.
